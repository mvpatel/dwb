{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Casket list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Caskettype</th>
                <th>Ispublished</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('casket_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.casketType }}</td>
                <td>{{ entity.isPublished }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('casket_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('casket_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('casket_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
